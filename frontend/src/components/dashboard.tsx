// dashboard.tsx - Simple dashboard placeholder component
// This is a basic component that shows when a user successfully logs in
// Note: The main dashboard functionality is in taskpopup.tsx

// Import React for JSX support
import React from "react";

// Define a simple functional component that shows a success message
const Dashboard: React.FC = () => {
  // Return a simple heading with a checkmark emoji to indicate successful login
  return <h1>Dashboard - Logged In Successfully</h1>;
};

// Export the component so it can be imported elsewhere
export default Dashboard;
